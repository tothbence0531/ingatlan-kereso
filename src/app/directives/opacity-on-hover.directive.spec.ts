import { OpacityOnHoverDirective } from './opacity-on-hover.directive';

describe('OpacityOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new OpacityOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
